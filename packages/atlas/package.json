{
  "name": "@cra/atlas",
  "version": "0.1.0",
  "description": "Atlas loader and validator for CRA",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "import": "./dist/index.js",
      "types": "./dist/index.d.ts"
    }
  },
  "scripts": {
    "build": "tsc",
    "clean": "rm -rf dist",
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@cra/protocol": "*",
    "ajv": "^8.12.0",
    "yaml": "^2.3.0"
  },
  "devDependencies": {
    "typescript": "^5.3.0",
    "vitest": "^1.1.0"
  },
  "files": [
    "dist"
  ]
}
